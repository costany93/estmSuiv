{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <h1>Ajouter</h1>
        {{form_start(form)}}
            {{form_widget(form)}}
            <button type="submit"  class="btn btn-primary">
                Créer l'activité
            </button>
        {{form_end(form)}}
    </div>
{% endblock %}