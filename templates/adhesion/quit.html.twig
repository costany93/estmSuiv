{% extends 'base.html.twig' %}

{% block title %}Hello AdhesionController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h5>J'ai décidé de quitter le club {{club.nom}}</h5>
                {{form_start(form)}}
                    {{form_widget(form)}}
                    <button type="submit" class="btn btn-danger">Je quitte le club</button>
                {{form_end(form)}}
            </div>
            <div class="col-md-6">

            </div>
        </div>
    </div>
{% endblock %}
