{% extends 'base.html.twig' %}

{% block title %}Hello AdhesionController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h5>J'ai décidé d'adhérer au club' {{club.nom}}</h5>
                {{form_start(form)}}
                    {{form_widget(form)}}
                    <button type="submit" class="btn btn-success">J'adhère au club</button>
                {{form_end(form)}}
            </div>
            <div class="col-md-6">

            </div>
        </div>
    </div>
{% endblock %}
